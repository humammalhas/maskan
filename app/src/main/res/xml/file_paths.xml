<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
         Only the sweep directory used for sharing. Nothing else in the app's storage is
         reachable through the provider, and the encrypted originals never leave filesDir.
    -->
    <cache-path name="shared_images" path="shared_images/" />
</paths>